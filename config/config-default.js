var config = {}

config.dir  = '/Dropbox/Apps/Poormans/';
config.file = 'poormans.md';
config.port = 1234;

config.logLevel = 'info';

config.dropbox = {
    key    : '',
    secret : '',
    token  : ''
};

module.exports = config;
